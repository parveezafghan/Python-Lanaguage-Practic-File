# a list can be homegeneous(1234) or heterogeneous(name.123,) index in the list we access number.poitive(1,2,3,) or negetive(-1,-2,-3)
# empty list (list.data type.())  .for loop .location print from list by index number.jumping.
# id (id in list in strage have differece ) is or is not use for id comparison.in or in not use for finding elements 
# (==.!=)use for two list comparison.unpacking use with variable .nested list which make between another list.
# printing in list index by three ways.printing in for loop .adding .replication .copy.extend.append.insert.(index num)
# remove use for delet .pop use delet final element or (index number).clear use to delet all data.del.assgi
# ment.sot.resor.reverse in sort.count.maximam number(max).maniman number(main)if we use max and main in strange thy follow first character in word  not full word
# index find number.ord use to print strange  value.even ,odd,lsit,if we have two empty list we can pass value 50 (25 ,25 )/.
# empty list list=list()in we can pass one parametar


list3=[[1,2,3,4],["parveez",'king','mobeen'],[100,200,300,4000]]


list4=[1,2,3,4,5,6]



print(1 in list3)


print(1 not in list3)
#tuple/not changable /tuple can be homogenece and hetrogenece/keyword tuple=()/tuplicate value allowable /type/
# for useing/not mutible/positive and negitive index/impty tuple(tuple=()/mt=tuple(constractor))/value pass to empty tuple
#printing tuple by 3 way(by index ,for loop,unpacking)\adding data by for.range(odd,even)/
#if we want to add data to tuple we should tuple change to list then we can add data after data add we can change list to tuple most important trak
#id for printing memory address/is is not memory comprasion/== != data comparison/in not in for data contain.
#concatination use to join two tuple/replication(tuple*1)/count/mainamam.maximam/ord use find code digit/
#sorting tuple if we want to sor tuple should change tuple to list then we can sort list then we should convert list to tuple 
#sorting (reverse=True) follow above detials/


tuple1=(100,200,300)
tuple2=(100,200,300)









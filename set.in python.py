#set in python collection which is unordered and unindexid in python
#point to be noted/not allow duplicate value/isertion order is not preserve( location loss)/no positive and negitive index/mutable modification is posible
#empty set/type/follow immutiable data/add/add tuple to set/update use to pass more then one and two value to set.
#list pass to set not allow but by update we can adde /deferernce bewteen update and add.add as hole data and update as eliment by eliment by the update we use list add to set/
#add follow immutible data/copy()by copy we can add data into empty set # set2=set1.copy()/ for loop with range/
#print(set1-set2)same elimint sub\print(set1^set2)same elimint sub/print(set1 | n set2)duplicate show/print(set1&set2)same show/remove/pop/discard//clear.conversion/sorted/reverse=True//lenght(len)


set1=[]
set=[x for x in range(0,20)]
print(set)

for x in set:
    if(x%2==0):
       set1.append(x)
    else:
        set1.append(x)
    print(set1)
 
    print(set1)